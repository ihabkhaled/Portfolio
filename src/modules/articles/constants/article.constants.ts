/** Gateway path suffixes — the only place article endpoints are spelled. */
export const ARTICLE_ENDPOINTS = {
  list: 'articles',
  create: 'articles',
} as const;

export const ARTICLES_DEFAULT_PAGE = 1;
export const ARTICLES_DEFAULT_PAGE_SIZE = 10;
